{
  "name": "cekio-contracts",
  "version": "0.1.0",
  "description": "On-chain post-dated cheque (çek) smart contracts with yield-bearing escrow",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "solc": "^0.8.24",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
